@model IngredienteViewModel

@{
    ViewData["Title"] = "Crear Ingrediente";
}

<h2>Crear Nuevo Ingrediente</h2>

<form asp-action="Crear" method="post" class="row g-3 col-md-6">
    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

    <div class="col-12">
        <label for="nombre" class="form-label">Nombre</label>
        <input asp-for="Nombre" class="form-control" />
        <span asp-validation-for="Nombre" class="text-danger"></span>
    </div>

    <div class="col-12 form-check">
        <input asp-for="Activo" type="checkbox" class="form-check-input" checked="checked" />
        <label asp-for="Activo" class="form-check-label">Activo</label>
    </div>

    <div class="col-12">
        <button type="submit" class="btn btn-primary">Crear</button>
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
